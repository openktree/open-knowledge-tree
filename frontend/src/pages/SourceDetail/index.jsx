// Route entry for /:slug/sources/:sourceID.
// The implementation lives in SourceDetailPage.jsx so this
// file stays under the page-size-policy limits. The
// router resolves this default export automatically.
export { default } from "./SourceDetailPage";
