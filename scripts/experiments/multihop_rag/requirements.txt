datasets>=2.14
httpx>=0.27
tqdm>=4.66
# Baselines (Dense X + Traditional RAG) dependencies.
# qdrant-client talks to the already-running Qdrant instance directly
# (no OKT backend changes). numpy is a qdrant-client transitive dep
# pinned explicitly for reproducibility.
qdrant-client>=1.9
numpy>=1.24